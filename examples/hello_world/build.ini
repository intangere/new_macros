[build]
packages = .
entry = main.go

[config]
pre-clean = true
merge = auto ; options are `ask` or `auto`. omitting this means no merge
keep-generated = false
backup = true
backup-dir = ~/latest_backup
